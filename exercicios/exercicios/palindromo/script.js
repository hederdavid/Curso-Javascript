//alert("Um palíndromo é uma palavra que pode ser lida da mesma forma de trás para frente. Ex(Arara)")
let palindromo = prompt(("Insira uma palavra:"))

for (let i = 0; i < palindromo.length; i++){
    if (palindromo.charAt(i) === palindromo.length(-1))
}

